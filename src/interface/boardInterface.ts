export interface boardDataInfo {
  no: number;
  title: string;
  content: string;
  writer: string;
  createDate: string;
}

export interface CreateFormInfo {
  title: string;
  content: string;
  writer: string;
  createDate: string;
}
